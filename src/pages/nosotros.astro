---
// src/pages/nosotros.astro
export const prerender = true;
import PageLayout from '@/layouts/PageLayout.astro';
import { HeroServices } from '@/components/sections/HeroServices';
import Button from '@/components/ui/Button.astro';
import Card from '@/components/ui/Card.astro';
import { Award, Users, Heart, Clock } from 'lucide-react'; // Iconos para valores
import { getImage, Image } from 'astro:assets';
import aboutHeroDesktop from '@/assets/images/about-hero-desktop.png';
import aboutHeroTablet from '@/assets/images/about-hero-tablet.png';
import aboutHeroMobile from '@/assets/images/about-hero-mobile.png';
import teamWorking from '@/assets/images/team-working.png';
import pattern from '@/assets/images/pattern.png';

const stats = [
  { number: '+3', label: 'Años de Experiencia', icon: Clock },
  { number: '+100', label: 'Eventos Realizados', icon: Award },
  { number: '100%', label: 'Clientes Satisfechos', icon: Heart },
  { number: '10+', label: 'Bartenders Certificados', icon: Users },
];

const values = [
  {
    title: 'Excelencia',
    desc: 'No aceptamos nada menos que la perfección en cada cóctel que servimos.'
  },
  {
    title: 'Personalización',
    desc: 'Creemos que cada evento debe tener su propia identidad y sabor único.'
  },
  {
    title: 'Hospitalidad',
    desc: 'Servimos personas, no solo bebidas. La calidez es nuestro sello.'
  }
];

const optDesktop = await getImage({ src: aboutHeroDesktop, format: 'webp', width: 1920 });
const optTablet = await getImage({ src: aboutHeroTablet, format: 'webp', width: 1024 });
const optMobile = await getImage({ src: aboutHeroMobile, format: 'webp', width: 640 });
const optPattern = await getImage({ src: pattern, format: 'webp', width: 100 });
---

<PageLayout 
  title="La Reserva | Experiencias de Coctelería para Eventos en Lima" 
  description="Conoce La Reserva, una empresa especializada en experiencias de coctelería para bodas, cumpleaños y eventos privados en Lima."
>

  {/* 1. HERO DE NOSOTROS */}
  <HeroServices
    client:load
    title="Nuestra Historia"
    subtitle="Más que bartenders, somos creadores de experiencias. Descubre la pasión detrás de cada copa."
    imageDesktop={optDesktop.src}
    imageTablet={optTablet.src}
    imageMobile={optMobile.src}
  />

  {/* 2. HISTORIA Y MISIÓN */}
  <section class="section bg-white">
    <div class="container-custom">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        
        {/* Texto */}
        <div class="space-y-6">
          <span class="text-primary-600 font-bold tracking-widest uppercase text-sm">
            Nuestra Esencia
          </span>
          <h2 class="text-4xl font-display font-bold text-secondary-900 leading-tight">
            Elevando el Arte de la <br/>
            <span class="text-primary-500">Hospitalidad en Lima</span>
          </h2>
          <div class="space-y-4 text-secondary-600 text-lg leading-relaxed">
            <p>
              La Reserva nació hace hace 3 años con una visión clara: transformar el servicio de bar en eventos de un simple "agregado" a una <strong>experiencia protagonista</strong>.
            </p>
            <p>
              Entendimos que un buen cóctel tiene el poder de romper el hielo, iniciar conversaciones y crear memorias. Por eso, no solo mezclamos licores; diseñamos momentos.
            </p>
            <p>
              Hoy, somos referentes en el sector de bodas y eventos corporativos de lujo, manteniendo siempre nuestra promesa: calidad premium, estética impecable y un servicio que hace sentir especial a cada invitado.
            </p>
          </div>
          
          <!-- <div class="pt-4">
            <img src="/images/signature.png" alt="Firma Fundador" class="h-12 opacity-60" /> {/* Opcional: firma decorativa */}
          </div> -->
        </div>

        {/* Imagen / Collage */}
        <div class="relative">
          <div class="aspect-[4/5] rounded-lg overflow-hidden shadow-2xl">
            <Image 
              src={teamWorking} 
              alt="Equipo de bartenders de La Reserva atendiendo y preparando coctelería en un evento en Lima" 
              class="w-full h-full object-cover hover:scale-105 transition-transform duration-700"
            />
          </div>
          {/* Cuadro decorativo flotante */}
          <div class="absolute -bottom-10 -left-10 bg-secondary-900 text-white p-8 rounded-lg shadow-xl max-w-xs hidden md:block">
            <p class="font-display italic text-xl leading-relaxed">
              "La mixología es el arte de poner el alma en una copa."
            </p>
          </div>
        </div>

      </div>
    </div>
  </section>

  {/* 3. ESTADÍSTICAS */}
  <section class="py-20 bg-secondary-900 text-white relative overflow-hidden">
    {/* Patrón de fondo */}
    <div class="absolute inset-0 opacity-10" style={`background-image: url('${optPattern.src}')`}></div>
    
    <div class="container-custom relative z-10">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8 text-center">
        {stats.map(stat => (
          <div class="p-4 group">
            <div class="mb-4 flex justify-center text-primary-500 group-hover:scale-110 transition-transform duration-300">
              <stat.icon className="w-10 h-10" />
            </div>
            <div class="text-4xl md:text-5xl font-bold mb-2 text-white">
              {stat.number}
            </div>
            <div class="text-sm md:text-base text-secondary-300 uppercase tracking-wider">
              {stat.label}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  {/* 4. VALORES */}
  <section class="section bg-secondary-50">
    <div class="container-custom">
      <div class="text-center max-w-3xl mx-auto mb-16">
        <h2 class="text-3xl md:text-4xl font-display font-bold text-secondary-900 mb-6">
          Nuestros Pilares
        </h2>
        <p class="text-secondary-600">
          Lo que nos guía en cada evento que realizamos.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {values.map(val => (
          <Card class="text-center p-8 hover:-translate-y-2 transition-transform duration-300 border-t-4 border-t-primary-500">
            <h3 class="text-xl font-bold text-secondary-900 mb-4">{val.title}</h3>
            <p class="text-secondary-500 leading-relaxed">
              {val.desc}
            </p>
          </Card>
        ))}
      </div>
    </div>
  </section>

  {/* 5. CTA */}
  <section class="py-24 bg-white relative">
    <div class="container-custom text-center">
      <h2 class="text-3xl md:text-5xl font-display font-bold text-secondary-900 mb-8">
        ¿Te gustaría trabajar con nosotros?
      </h2>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button variant="primary" href="/cotizacion" size="lg">
          Cotizar mi Evento
        </Button>
        <Button variant="outline" href="/contacto" size="lg" class="border-secondary-300 text-secondary-600 hover:border-secondary-900 hover:text-secondary-900">
          Contactar al Equipo
        </Button>
      </div>
    </div>
  </section>

</PageLayout>